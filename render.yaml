services:
  - type: web
    name: investment-analyzer
    env: node
    rootDir: investment-analyzer
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
